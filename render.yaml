services:
  - type: web
    name: personel-premium-server
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --workers 1 --threads 8 --timeout 60 --keep-alive 5 --preload app:app
